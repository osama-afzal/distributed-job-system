export enum JobStatus {
    Pending = 'pending',
    Processing = 'processing',
    Completed = 'completed',
    Failed = 'failed'
}

export enum JobType {
    Report = 'report',
    Email = 'email',
    Analytics = 'analytics'
}